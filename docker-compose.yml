services:
  db:
    image: dhi.io/mysql:lts-debian13
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw
    command: mysqld
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data:
